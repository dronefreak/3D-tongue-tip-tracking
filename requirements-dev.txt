# Development requirements for 3D Tongue Tip Tracking
# Install with: pip install -r requirements-dev.txt

# Includes runtime dependencies
-r requirements.txt

# Testing
pytest>=7.0
pytest-cov>=4.0

# Linting + formatting (replaces flake8 / black / pylint)
ruff>=0.4

# Pre-commit hooks
pre-commit>=3.0
